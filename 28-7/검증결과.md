# 28-7 검증결과

모든 수치는 실제 명령 출력을 그대로 인용함.

## 타입 검사

`npx tsc --noEmit`:

```
tsc exit: 0
```

## vitest

`npx vitest run`:

```
      Tests  115 passed (115)
```

(28-6은 104개 → 입찰 모델 6·추가 작업 5·상태 머신 lost 등 11개 추가, 수수료 3% 기대값 전면 갱신)

## node --test

`npm run test:node`:

```
# pass 7
# fail 0
```

## 반복 실행 (흔들림 확인, 3회)

```
      Tests  115 passed (115)
# pass 7
# fail 0
      Tests  115 passed (115)
# pass 7
# fail 0
      Tests  115 passed (115)
# pass 7
# fail 0
```

3회 모두 동일 — 흔들림 없음.

## 빌드

`npm run build`:

```
dist/assets/index-C7cppxM5.js   177.66 kB │ gzip: 56.18 kB
✓ built in 1.32s
```

## 이전 라운드 무변경 해시 검증

28-7 생성 *전* `for d in 28-*` glob으로 28-1~28-6 전체 177개 파일의 md5 목록을 baseline으로 저장, 생성 *후* 동일 파일 목록으로 재생성해 diff:

```
HASH-IDENTICAL: 28-1~28-6 무변경 확인 (177개 파일)
```

## 배포 검증 (Vercel 동일 조건)

`NODE_ENV=production npm ci --include=dev && npm run build` (repo 28-7):

```
✓ built in 1.22s
DIST-OK
```

vercel.json은 28-7을 가리키도록 갱신됨.
