# 28-5 검증결과

모든 수치는 실제 명령 출력을 그대로 인용함.

## 타입 검사

`npx tsc --noEmit`:

```
tsc exit: 0
```

## vitest

`npx vitest run`:

```
      Tests  95 passed (95)
```

(28-4는 87개 → 슬롯 해제 3개·백업 3개·대시보드 집계 2개 추가)

## node --test

`npm run test:node`:

```
# pass 7
# fail 0
```

## 반복 실행 (흔들림 확인, 3회)

```
      Tests  95 passed (95)
# pass 7
# fail 0
      Tests  95 passed (95)
# pass 7
# fail 0
      Tests  95 passed (95)
# pass 7
# fail 0
```

3회 모두 동일 — 흔들림 없음.

## 빌드

`npm run build`:

```
dist/assets/index-N1oy8C8P.js   168.24 kB │ gzip: 54.04 kB
✓ built in 1.22s
```

## 이전 라운드 무변경 해시 검증

28-5 생성 *전* `for d in 28-*` glob으로 28-1~28-4 전체 115개 파일(28-4/감사결과.md 포함)의 md5 목록을 baseline으로 저장, 생성 *후* 동일 파일 목록으로 재생성해 diff:

```
HASH-IDENTICAL: 28-1~28-4 무변경 확인 (115개 파일)
```

## 배포 검증 (Vercel 동일 조건)

`NODE_ENV=production npm ci --include=dev && npm run build` (repo 28-5):

```
✓ built in 1.13s
DIST-OK
```

vercel.json은 28-5를 가리키도록 갱신됨.
