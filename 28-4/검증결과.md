# 28-4 검증결과

모든 수치는 실제 명령 출력을 그대로 인용함.

## 타입 검사

`npx tsc --noEmit`:

```
tsc exit: 0
```

## vitest

`npx vitest run`:

```
      Tests  87 passed (87)
```

(28-3은 72개 → 영속화 7개·일정 통합 5개·정산 구분 3개 등 15개 추가)

## node --test

`npm run test:node`:

```
# pass 7
# fail 0
```

## 반복 실행 (흔들림 확인, 3회)

```
      Tests  87 passed (87)
# pass 7
# fail 0
      Tests  87 passed (87)
# pass 7
# fail 0
      Tests  87 passed (87)
# pass 7
# fail 0
```

3회 모두 동일 — 흔들림 없음.

## 빌드

`npm run build`:

```
dist/assets/index-D4fsQPJE.js   164.05 kB │ gzip: 52.89 kB
✓ built in 1.31s
```

## 이전 라운드 무변경 해시 검증

28-4 생성 *전* `for d in 28-*` glob으로 28-1~28-3 전체 84개 파일의 md5 목록을 baseline으로 저장, 생성 *후* 동일 파일 목록으로 재생성해 diff:

```
HASH-IDENTICAL: 28-1~28-3 무변경 확인 (84개 파일)
```

## 배포 검증 (Vercel 동일 조건)

`NODE_ENV=production npm ci --include=dev && npm run build` (repo 28-4):

```
✓ built in 1.15s
DIST-OK
```

vercel.json은 28-4를 가리키도록 갱신됨.
