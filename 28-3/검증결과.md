# 28-3 검증결과

모든 수치는 실제 명령 출력을 그대로 인용함.

## 타입 검사

`npx tsc --noEmit`:

```
tsc exit: 0
```

## vitest

`npx vitest run`:

```
      Tests  72 passed (72)
```

(28-2는 57개 → platform.test.ts 12개 신설 및 money.test.ts 확장)

## node --test

`npm run test:node`:

```
# pass 7
# fail 0
```

## 반복 실행 (흔들림 확인, 3회)

```
      Tests  72 passed (72)
# pass 7
# fail 0
      Tests  72 passed (72)
# pass 7
# fail 0
      Tests  72 passed (72)
# pass 7
# fail 0
```

3회 모두 동일 — 흔들림 없음.

## 빌드

`npm run build`:

```
dist/assets/index-DDE3qDFp.js   158.85 kB │ gzip: 51.50 kB
✓ built in 1.33s
```

## 이전 라운드 무변경 해시 검증

28-3 생성 *전* `for d in 28-*` glob으로 28-1·28-2 전체 53개 파일의 md5 목록을 baseline으로 저장, 생성 *후* 동일 파일 목록으로 재생성해 diff:

```
HASH-IDENTICAL: 28-1, 28-2 무변경 확인 (53개 파일)
```

(diff 출력 없음 = 53개 파일 전부 해시 동일)

## 배포 검증

Vercel과 동일 조건(NODE_ENV=production, `npm ci --include=dev`)의 로컬 빌드 검증은 커밋 전 수행 — 결과는 커밋 로그 참조. vercel.json은 28-3을 가리키도록 갱신됨.
