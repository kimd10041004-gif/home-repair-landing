# 28-6 검증결과

모든 수치는 실제 명령 출력을 그대로 인용함.

## 타입 검사

`npx tsc --noEmit`:

```
tsc exit: 0
```

## vitest

`npx vitest run`:

```
      Tests  104 passed (104)
```

(28-5는 95개 → 활성 일정 2·백업 검증 5·월별 리포트 2·메모 1 등 9개 추가)

## node --test

`npm run test:node`:

```
# pass 7
# fail 0
```

## 반복 실행 (흔들림 확인, 3회)

```
      Tests  104 passed (104)
# pass 7
# fail 0
      Tests  104 passed (104)
# pass 7
# fail 0
      Tests  104 passed (104)
# pass 7
# fail 0
```

3회 모두 동일 — 흔들림 없음.

## 빌드

`npm run build`:

```
dist/assets/index-C36q2VoJ.js   171.77 kB │ gzip: 54.82 kB
✓ built in 1.12s
```

## 이전 라운드 무변경 해시 검증

28-6 생성 *전* `for d in 28-*` glob으로 28-1~28-5 전체 146개 파일(28-5/감사결과.md 포함)의 md5 목록을 baseline으로 저장, 생성 *후* 동일 파일 목록으로 재생성해 diff:

```
HASH-IDENTICAL: 28-1~28-5 무변경 확인 (146개 파일)
```

## 배포 검증 (Vercel 동일 조건)

`NODE_ENV=production npm ci --include=dev && npm run build` (repo 28-6):

```
✓ built in 1.04s
DIST-OK
```

vercel.json은 28-6을 가리키도록 갱신됨.
