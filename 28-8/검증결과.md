# 28-8 검증결과

모든 수치는 실제 명령 출력을 그대로 인용함.

## 타입 검사

`npx tsc --noEmit`:

```
tsc exit: 0
```

## vitest (도메인 무변경 — 기존 테스트가 회귀 안전망)

`npx vitest run`:

```
      Tests  115 passed (115)
```

## node --test

`npm run test:node`:

```
# pass 7
# fail 0
```

## 반복 실행 (흔들림 확인, 3회)

```
      Tests  115 passed (115)
# pass 7
# fail 0
      Tests  115 passed (115)
# pass 7
# fail 0
      Tests  115 passed (115)
# pass 7
# fail 0
```

3회 모두 동일 — 흔들림 없음.

## 빌드

`npm run build`:

```
dist/assets/index-Br6uqK1D.css    5.66 kB │ gzip:  1.80 kB
dist/assets/index-DDDW2xwN.js   178.76 kB │ gzip: 56.46 kB
✓ built in 1.24s
```

## 시각 렌더링 검증

`vite preview` + Playwright(Chromium 1194)로 대시보드 / 일감·입찰 / 정산 탭 전체 페이지 스크린샷 촬영 — 네이비 헤더, 4지표 타일, 상태 칩 컬러, D-day(D-1·D-2 주황), 2열 입찰 폼, 월별/주차 정산 표 렌더링 정상 확인.

## 이전 라운드 무변경 해시 검증

28-8 생성 *전* `for d in 28-*` glob으로 28-1~28-7 전체 208개 파일의 md5 목록을 baseline으로 저장, 생성 *후* 동일 파일 목록으로 재생성해 diff:

```
HASH-IDENTICAL: 28-1~28-7 무변경 확인 (208개 파일)
```

## 배포 검증 (Vercel 동일 조건)

`NODE_ENV=production npm ci --include=dev && npm run build` (repo 28-8):

```
✓ built in 1.15s
DIST-OK
```

vercel.json은 28-8을 가리키도록 갱신됨.
