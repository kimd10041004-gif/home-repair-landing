# 28-9 검증결과

모든 수치는 실제 명령 출력을 그대로 인용함.

## 타입 검사

`npx tsc --noEmit`:

```
tsc exit: 0
```

## vitest

`npx vitest run`:

```
      Tests  124 passed (124)
```

(28-8은 115개 → 체크리스트 5·프로필 4 등 9개 추가)

## node --test

`npm run test:node`:

```
# pass 7
# fail 0
```

## 반복 실행 (흔들림 확인, 3회)

```
      Tests  124 passed (124)
# pass 7
# fail 0
      Tests  124 passed (124)
# pass 7
# fail 0
      Tests  124 passed (124)
# pass 7
# fail 0
```

3회 모두 동일 — 흔들림 없음.

## 빌드

`npm run build`:

```
dist/assets/index-DxD729Q3.css    7.24 kB │ gzip:  2.18 kB
dist/assets/index-KQM80MNn.js   183.33 kB │ gzip: 57.68 kB
✓ built in 1.23s
```

## 시각 렌더링 검증

`vite preview` + Playwright(Chromium)로 확인:
- 내 작업 탭: 진행 중 카드(낙찰 단계 강조 + 체크리스트 3항목)와 정산 완료 이력 카드(전 단계 완료 표시) 스텝퍼 정상.
- 마이 탭: 프로필 폼 렌더링 정상.
- 모바일(390×800): 하단 고정 탭바 6탭 정상, 지표 타일 2열 래핑 정상.

## 이전 라운드 무변경 해시 검증

28-9 생성 *전* `for d in 28-*` glob으로 28-1~28-8 전체 239개 파일의 md5 목록을 baseline으로 저장, 생성 *후* 동일 파일 목록으로 재생성해 diff:

```
HASH-IDENTICAL: 28-1~28-8 무변경 확인 (239개 파일)
```

## 배포 검증 (Vercel 동일 조건)

`NODE_ENV=production npm ci --include=dev && npm run build` (repo 28-9):

```
✓ built in 1.27s
DIST-OK
```

vercel.json은 28-9를 가리키도록 갱신됨.
